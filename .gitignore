node_modules/
dist/
build/
coverage/
.turbo/

# Ambiente — nunca commitar segredos
.env
.env.*
!.env.example

# Prisma
apps/api/prisma/*.db
apps/api/prisma/migrations/dev.db*

# Storage local de mídia (Fase 3).
# A barra inicial ancora o padrão na raiz do repositório. Sem ela, "storage/"
# casaria com qualquer diretório de mesmo nome em qualquer nível — foi assim
# que o código-fonte de apps/api/src/modules/storage/ ficou fora do
# versionamento, quebrando o build em clone limpo.
/storage/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editores / SO
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Agente — pode conter credenciais
.claude/settings.local.json
